{
  "name": "scribe-auth-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:push": "wrangler d1 execute scribe-auth-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute scribe-auth-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "better-auth": "^1.7.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "wrangler": "^4.0.0"
  }
}
